#include <stdio.h>
#include "hello.h"

int hello_world() { //함수, int는 함수의 타입
	printf("hello, World!\n"); //문자(배)열, 구문을 의미
	return 0; //반환 값

	//문자열 = 문자(배열) char[15] 
	//문자열의 첫글자로 배열의 주소를 파악함 = 배열의 이름
}